var RuleParser = function () {

    /**
     * Returns a set of Rules containing all the rules from the database,
     * or throws an Exception if the databse is invalid.
     */
    this.parseRules = function (database) {
        return Set([]);
    }

}

module.exports = RuleParser;
